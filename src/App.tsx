/**
 * Placeholder de infraestructura — confirma que el scaffold (Vite + React +
 * design tokens) arranca correctamente. Se reemplaza por el panel real
 * cuando se implemente la primera historia de Administrador, siguiendo el
 * plan previo (DOCS/context.md, sección 12).
 */
function App() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: 'var(--color-beige)',
        fontFamily: 'var(--font-body)',
      }}
    >
      <div style={{ textAlign: 'center' }}>
        <h1 style={{ fontFamily: 'var(--font-heading)', color: 'var(--color-navy)' }}>
          MediRuta
        </h1>
        <p style={{ color: 'var(--color-teal)' }}>
          Infraestructura lista — pendiente de implementar la primera historia de Administrador
        </p>
      </div>
    </main>
  )
}

export default App
